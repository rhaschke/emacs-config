;;; some general settings
(setq inhibit-splash-screen t)
(setq make-backup-files nil) ;; do not create backup files *~
(setq kill-whole-line 1) ;; kill-line kills whole line
;(setq-default require-final-newline "ask")
;(setq use-dialog-box t) ;; use dialogs for yes-no-questions?
;(setq mouse-scroll-delay 50) ;; slower mouse scrolling
(setq-default word-wrap t) ;; wrap long lines at word boundaries

